<resources>
    <!-- Base application theme. -->
    <style name="Theme.FootballLivescore" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">#FFFFFF</item>
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:windowBackground">@color/white_dark</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/purple_dark</item>
        <!-- Customize your theme here. -->
        <item name="titleTextColor">@android:color/white</item>
        <item name="android:homeAsUpIndicator">@drawable/arrow_white</item>
    </style>

    <style name="Theme.FootballLivescore.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FootballLivescore.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FootballLivescore.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>